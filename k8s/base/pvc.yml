apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: claude-usage-dashboard-state
  labels:
    app.kubernetes.io/name: claude-usage-dashboard
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
