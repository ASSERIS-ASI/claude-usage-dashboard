# Entrypoints

- `dashboard.js` — local dashboard server.
- `jsonl-agent.js` — local Claude JSONL scanner.
- `provider-agent.js` — optional public release/outage metadata refresh.

Use the root `dashboard.js` launcher for normal operation.
